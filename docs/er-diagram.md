# ER Diagram

The Prisma schema defines the core entities: User, Organization, Project, Queue, Job, JobExecution, JobLog, ScheduledJob, Worker, WorkerHeartbeat, RetryPolicy, and DeadLetterJob.

A Mermaid diagram can be generated from the Prisma schema once the Prisma CLI is available.
